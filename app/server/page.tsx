import { SectionCard } from "@/components/shared/section-card";

export default function ServerPage() {
  return (
    <main className="mx-auto max-w-4xl p-8">
      <SectionCard
        title="Mentorly foundation status"
        description="The starter demo has been replaced with the Phase 1 identity layer. Future phases will add mentor discovery, session requests, and scheduling."
      />
    </main>
  );
}
